class Main {

    function void main(){

        var Fraction a,b,c;

        let a = Fraction.new(1, 2);
        let b = Fraction.new(1, 3);
        let c = a.plus(b);

        do Output.printString("result: ");
        do c.print();

        return;

    }

}